<?php
if (isset($_GET['id'])) {
  $list = array();
  


  // check if id is in database

  // see what type
  // add coordinates to array
  // send result
} else {
  echo '{"type" : "fail"}';
  die("No idea sent");
}
?>
